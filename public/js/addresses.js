$( document ).ready(function() {
  setClickEvents();
});

function setClickEvents(){

	/*$.("#add-address-button").click(function(e) {

	    e.preventDefault();
	    
	    // Get the new address in the text box
	    var addresses = [];
	    addresses.push( $("#add-address-input").val());

	    // Iterate over existing addresses and put them in the array
	    $( "ul#address-list li" ).each(function( index ) {
		  addresses.push( $( this ).text() );
		});

		// Set the addresses as a request param

	    return false;  
	});	*/
}
